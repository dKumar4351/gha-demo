name: Hello World
on: workflow_dispatch
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: |
          # echo "Welcome to GHA"
          echo "This is first line"
          echo "This is line two"
