## feature/** --> static code analysis
## develop --> Deploy the code to development environment
## main --> Deploy the code to production environment
## send success notification
## send failure notification

name: Nice If Examples
on: 
  push:
    branches:
      - main
      - develop
      - feature/**
jobs: 
  quality_checks:
    if: startsWith(github.ref_name,'feature') && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Perform code quality
        run: echo "Performing quality checks on the code"

  dev-deploy:
    if: github.ref_name == 'develop' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Dev Deploy
        run: echo "Performing deployment of the code on dev"

  prod_deploy:
    if: github.ref_name == 'main') && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: prod Deploy
        run: echo "Performing deployment of the code on prod"
  success_notify: 
      needs: ['quality_checks','dev_deploy','prod_deploy']
      if: always() && !failure()
      runs-on: ubuntu-latest
      steps: 
        - name: Notify success
          run: echo "success email sent to the team"

  failure_notify: 
      needs: ['quality_checks','dev_deploy','prod_deploy']
      if: failure()
      runs-on: ubuntu-latest
      steps: 
        - name: Notify Failure
          run: echo "Deployment pipeline has failed" 